<div id="page-wrapper">
	<div class="graphs">
		<h3 class="blank1">Shop Creation</h3>
		<%= form_for(:shop, :url => {:action => 'create', :id => @shop.id}) do |f| %>

			<% @action = "edit" %>

		    <%= render(:partial => "form", :locals => {:f => f}) %>
		    <div class="row">
		    	<div calss="col-sm-8 col-sm-offset-2">
				    
				    <%= submit_tag("Submit", :class => "btn-primary btn") %>
				    <%= link_to("Cancel",{:controller => 'shop', :action => 'index'}, :class => "btn-danger btn") %>
				</div>
			</div>
			<br/><br/>
		<% end %>
	</div>
</div>